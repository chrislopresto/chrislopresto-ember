machine:
  node:
    version: 0.10.21
dependencies:
  pre:
    - gem install --pre sass-css-importer
    - gem install --pre compass
    - npm install -g bower
    - bower install
general:
  branches:
    ignore:
      - gh-pages
